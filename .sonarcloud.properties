# SonarQube Cloud Automatic Analysis reads THIS file (.sonarcloud.properties);
# it IGNORES sonar-project.properties. Keep the two in sync.
#
# Exclusions:
#   - _bsv_native/secp256k1/**  vendored upstream libsecp256k1 sources. Its
#     dev tooling (test-vector generators) trips security rules for path
#     handling — third-party code we do not maintain, so it must not gate our
#     Quality Gate (Security Rating on New Code).
#   - pyproject.toml            S8565 (lock file) false positive; this project
#     pins deps via setuptools + uv.lock.
#   - .github/**                S8541/S8544 on pip install in CI workflows.
#     The project's C extension requires source builds (`pip install -e .`),
#     which is incompatible with --only-binary :all:. CI supply-chain
#     security is managed via pinned action SHAs + Dependabot.
sonar.exclusions=pyproject.toml,_bsv_native/secp256k1/**,.github/**
