# DEPRECATED: Use wheels.yml for publishing.
# This workflow is kept for backward compatibility and will be removed.
# wheels.yml builds platform-specific wheels via cibuildwheel and publishes on tag push.
name: Publish to PyPI (legacy)

permissions: {}

on:
  # Disabled — wheels.yml handles tag-based publishing
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow is deprecated. Use wheels.yml instead."
          echo "Push a tag (v*.*.*) to trigger the new multi-platform build + publish pipeline."
          exit 1
